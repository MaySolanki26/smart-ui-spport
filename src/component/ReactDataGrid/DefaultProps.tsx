/** @format */

const behavior = {
  columnResizeMode: "growAndShrink",
};

const sorting = {
  enabled: true,
};

const editing = {
  enabled: true,
  mode: "cell",
};

const appearance = {
  alternationCount: 2,
  showRowHeaderNumber: true,
  allowRowDetailToggleAnimation: true,
};

const layout = {
  rowHeight: 10,
};

const rowDetail = {
  enabled: true,
  visible: true,
  height: 120,
};

export { behavior, sorting, editing, appearance, layout, rowDetail };
